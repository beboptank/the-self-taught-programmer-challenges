#call a different function from the statistics module

import statistics

numberList = [5, 8, 10, 20, 31]

statistics.harmonic_mean(numberList)


#create a module named cubed with a function that takes a number as a parameter
#and returns the number cubed. Import and call the function from another module.

import cubed

cubed.cubeNumber(3)